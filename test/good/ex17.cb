def main() {
    let x = 0;
    let g = loop {
        if x > 10 {
            break "done"
        };
        printInt(x);
        printString("\n");
        x += 1;
    };
    printString(g);
    printString("\n");
}
