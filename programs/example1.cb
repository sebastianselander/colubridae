// declare an algebraic data type
type Foo {
    Bar,
    Foo,
    Baz(int),
}

def main() {
    let var = Foo;
    // pattern match
    match var {
        Foo => printString("got foo"),
        Bar => printString("got bar"),

        // The following two patterns have not been implemented yet
        // Baz(n) => printInt(n), 
        // x => let a: Foo = x;
    }
}
