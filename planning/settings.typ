#let header(nr) = [
    #align(center, text(17pt)[Planning report])
]

#let Header(nr) = [
    #header(nr)
    #grid(
        columns: (1fr),
        align(center)[
            Sebastian Selander \
            #link("mailto:gusselase@student.gu.se")
        ],
    )
]
